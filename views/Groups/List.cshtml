@model IEnumerable<Group>

<table class="table">
  <tr>
    <th scope="col">Id</th>
    <th scope="col">Name</th>
  </tr>
  @foreach (var item in Model)
  {
    <tr>
      <td>
        @item.Id
      </td>
      <td>
        @item.Name
      </td>

    </tr>
  }

</table>